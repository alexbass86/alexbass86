apiVersion: v1
kind: Service
metadata:
  name: {{ include "newrsdf.fullname" . }}
  labels:
    {{- include "newrsdf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "newrsdf.selectorLabels" . | nindent 4 }}
